# ----------------------------------------------------------------------
# Dockerfile - نسخه نهایی و پایدار
# ----------------------------------------------------------------------

# 1. تصویر پایه پایتون (سبک)
FROM python:3.11-slim-bullseye

# 2. نصب فقط بسته های سیستمی ضروری (مانند Locale)
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
    locales \
    && echo "en_US.UTF-8 UTF-8" > /etc/locale.gen && \
    locale-gen en_US.UTF-8 && \
    rm -rf /var/lib/apt/lists/*

# 3. تنظیم متغیرهای محیطی
ENV LANG en_US.UTF-8
ENV LC_ALL en_US.UTF-8
ENV PYTHONDONTWRITEBYTECODE 1
ENV PYTHONUNBUFFERED 1
ENV APP_HOME /usr/src/app
WORKDIR $APP_HOME

# 4. کپی و نصب وابستگی‌ها
COPY requirements.txt .
RUN pip install --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt

# 5. کپی کردن سورس کد برنامه
COPY . .

# 6. دستور اجرای نهایی و صریح (بدون متغیر محیطی $PORT)
# با تعیین صریح پورت 8080، خطای 'Invalid value for --port: $PORT' برطرف می‌شود.
CMD ["python", "-m", "uvicorn", "bot_app:app", "--host", "0.0.0.0", "--port", "8080"]
```eof

***

### ۲. گام‌های نهایی برای برقراری ارتباط (رفع خطای 404)

پس از دیپلوی مجدد با `Dockerfile` بالا، مشکل اجرای سرور حل خواهد شد و سرور شروع به گوش دادن می‌کند (مثل لاگ‌های موفقیت‌آمیز اولیه). حالا باید مشکل اصلی (عدم پاسخگویی ربات) را که به خطای **404 Not Found** مربوط است، حل کنید:

1.  **تنظیم متغیر محیطی `BOT_TOKEN`:** 🔐
    شما باید مطمئن شوید که توکن ربات شما در تنظیمات دیپلوی (به صورت امن) و به عنوان متغیر محیطی با نام **`BOT_TOKEN`** تنظیم شده باشد. اگر این متغیر تنظیم نشود، مسیر وب‌هوک در `bot_app.py` اشتباه خواهد بود و خطای 404 تکرار می‌شود.

2.  **تنظیم مجدد وب‌هوک:** 📡
    پس از دیپلوی موفق، باید با یک درخواست HTTP به API تلگرام، آدرس وب‌هوک را به آدرس جدید سرویس خود (به همراه توکن) تنظیم کنید. این کار را با فراخوانی لینک زیر در مرورگر یا از طریق ابزاری مانند Postman انجام دهید:

    ```
    https://api.telegram.org/bot<توکن_واقعی_شما>/setWebhook?url=https://<آدرس_سرویس_شما>/<توکن_واقعی_شما>&secret_token=YOUR_SECRET_TOKEN
    ```

با حل مشکل پورت و انجام این دو گام امنیتی/تنظیماتی، ربات شما فعال خواهد شد.














